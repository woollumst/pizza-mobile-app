const mockRepo = {
    findAll: jest.fn(),
    findById: jest.fn(),
    create: jest.fn(),
};

describe('Menu Service Tests', () => {
    test.todo('test Menu Service');
});

//const menuService = new menuService(mockRepo as any);